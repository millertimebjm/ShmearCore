@page "/Shmear/{name}"
@inject IJSRuntime JSRuntime

<PageTitle>Shmear: @name</PageTitle>

@name

@functions {
    [Parameter]
    public string name { get; set; }

    protected override async Task OnInitializedAsync()
    {
        //var connectionId = await JSRuntime.InvokeAsync<string>("getConnectionId", new { });
        //Console.WriteLine("Connection ID: " + connectionId);
        Console.WriteLine("Connection ID: " + "test");
    }
}